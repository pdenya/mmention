<div class="header_wrap">
	<h1 class="text-center">Highly Recommended League of Legends videos</h1>	
</div>
<ol class="breadcrumb vboffset0 text-center">
	<li class="active">The most referenced videos from the r/leagueoflegends and r/summonerschool subreddits.</li>
</ol>
<div class="container">
	<div class="row voffset3">
		<% @references.each do |ref| %>
			<div class="col-sm-6 voffset2">
				<div class="well no-border text-center">
					<iframe width="420" height="315" src="<%= ref.video.embed_url %>" frameborder="0" allowfullscreen></iframe>
					<div class="row text-left voffset2">
						<div class="col-sm-5">
							<%= link_to "#{ref.count} reddit comments", show_video_path(ref.video.id), class: 'btn btn-primary'  %>
						</div>
						<div class="col-sm-7 text-right">
							<% @subs_mentioning_video[ref.video.id].each do |subreddit| %>
								<span class="label label-info"><a href="/?subreddit=<%= subreddit %>">r/<%= subreddit %></a></span>
							<% end %>
						</div>
					</div>
				</div>
			</div>
		<% end %>
	</div>
	<div class="row vboffset2">
		<div class="col-sm-4">
			<a href="/?page=<%= @page - 1 %><%= "&subreddit=#{params[:subreddit]}" if params[:subreddit] %>" class="btn btn-block btn-lg btn-warning" <%= 'disabled="disabled"' if @page < 2 %>>&laquo; Prev</a>
		</div>
		<div class="col-sm-4">
			<h4 class="text-center text-white">
				Page <%= @page %>
			</h4>
		</div>
		<div class="col-sm-4">
			<a href="/?page=<%= @page + 1 %><%= "&subreddit=#{params[:subreddit]}" if params[:subreddit] %>" class="btn btn-block btn-lg btn-warning">Next &raquo;</a>
		</div>
	</div>
</div>
<script type="text/javascript">
	$(function() {
		$('.well.no-border').fitVids();
	})
</script>
